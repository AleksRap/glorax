mixin callback-btn(className, mod)
  - var cls = className ? `${className}__callback-btn` : '';
  - var clsMod = mod ? ` ${className}__callback-btn_${mod}` : '';

  button.callback-btn(data-open-modal class=cls + clsMod)
    span.callback-btn__text Заказать обратный звонок